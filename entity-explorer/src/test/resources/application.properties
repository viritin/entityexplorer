server.port=9999

# Load demo Customer data at startup. defer-datasource-initialization ensures
# the script runs after Hibernate has created the schema (the CUSTOMER table).
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:customerdata.sql
spring.jpa.defer-datasource-initialization=true

